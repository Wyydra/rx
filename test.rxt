(module
 (func $_start ()
  (let $cmd_add 1)
  (let $cmd_sub 2)
  (let $arg1 15)
  (let $arg2 27)
  (let $me (self))

  (let $msg (tuple $cmd_sub 15 27 $me))
  
  (send "math" $msg)
  (let $res (recv))
  (print $res)
 )
)
